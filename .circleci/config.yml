jobs:
  build:
    docker:
      - image: circleci/node:6.14.1
      - image: circleci/mysql:5.7
    working_directory: ~/the-product
    steps:
      - checkout
      - run: sudo apt install mysql-client
      - restore_cache:
          key: v1-dependencies-{{ checksum "package.json" }}
      - run: yarn install
      - save_cache:
          key: v1-dependencies-{{ checksum "package.json" }}
          paths:
            - node_modules
      - run:
          name: 'create mysql database'
          command: mysql -h 127.0.0.1 -u root -e "create database if not exists the_product;"
      - run: yarn build
      - run: yarn test:lint
      - run: node ./scripts/sync-database.js
      - run:
          name: 'yarn test:unit'
          command: yarn test:unit --ci --coverage --runInBand --verbose --testResultsProcessor="jest-junit"
          environment:
            JEST_JUNIT_OUTPUT: "reports/junit/js-test-results.xml"
      - store_test_results:
          path: reports/junit
      - store_artifacts:
          path: reports/junit


