{
  "private": true,
  "name": "@the-product/firebase",
  "version": "0.1.0",
  "main": "./dist/firebase-app/lib/entry-development.js",
  "scripts": {
    "start": "concurrently './scripts/build-firebase.js --watch' 'tsc -w' 'cd dist-firebase && DEBUG=the-product:* firebase serve'",
    "deploy": "./scripts/deploy-to-prod.js",
    "logs": "firebase functions:log",
    "build": "./scripts/build-firebase.js && tsc",
    "build:watch": "concurrently './scripts/build-firebase.js --watch' 'tsc -w'",
    "test": "echo passed",
    "test:lint": "echo passed",
    "test:unit": "echo passed"
  },
  "dependencies": {
    "firebase-admin": "^5.12.0",
    "firebase-functions": "^1.0.1",
    "klay": "2.5.0-alpha.84196bee"
  },
  "devDependencies": {
    "concurrently": "^3.5.1",
    "watch": "^1.0.2"
  }
}
