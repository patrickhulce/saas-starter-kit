{
  "private": true,
  "name": "@the-product/shared",
  "version": "0.1.0",
  "main": "./lib/index.ts",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc -w",
    "test": "npm run test:lint && npm run test:unit",
    "test:lint": "lint",
    "test:unit": "jest"
  },
  "config": {
    "tslint": {
      "rules": {
        "no-implicit-dependencies": false,
        "no-unsafe-any": false
      }
    }
  },
  "dependencies": {
    "klay": "3.0.0-alpha.4",
    "lodash": "^4.17.5",
    "stripe": "^6.3.0"
  },
  "devDependencies": {
    "@types/stripe": "^5.0.18"
  }
}
